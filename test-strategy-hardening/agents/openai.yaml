interface:
  display_name: "Test Strategy Hardening"
  short_description: "Increase confidence while simplifying test systems"
  default_prompt: "Use $test-strategy-hardening to solve the named test-system problem, prefer deletion and faithful boundaries, avoid test-layer inflation, and validate proportionally."
