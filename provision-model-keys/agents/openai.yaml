interface:
  display_name: "Provision Model Keys"
  short_description: "App keys, saved setup grants, and monthly budgets"
  default_prompt: "Use $provision-model-keys to set up this app's key and monthly starter budget within my saved grant."
