interface:
  display_name: "YouTube Channel Operator"
  short_description: "Design a safe multi-channel YouTube operator"
  default_prompt: "Use $youtube-channel-operator when the shared YouTube operations core, channel isolation, approvals, durable execution, or post-change measurement needs design or review."
policy:
  allow_implicit_invocation: true
