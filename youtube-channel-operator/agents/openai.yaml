interface:
  display_name: "YouTube Channel Operator"
  short_description: "Build YouTube operations, growth measurement, and approvals"
  default_prompt: "Use $youtube-channel-operator to design a YouTube workflow with raw API access, paid-versus-organic growth measurement, transcript tooling, and guided approvals."
policy:
  allow_implicit_invocation: true
