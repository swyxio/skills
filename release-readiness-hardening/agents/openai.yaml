interface:
  display_name: "Release Readiness Hardening"
  short_description: "Audit minimal deploy, rollback, and verification gates"
  default_prompt: "Use $release-readiness-hardening to audit the smallest sufficient release controls, simplify redundant machinery, and verify rollback and production health without adding ceremony by default."
