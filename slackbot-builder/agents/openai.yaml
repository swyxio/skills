interface:
  display_name: "Slackbot Builder"
  short_description: "Build causal, interactive production Slack bots and agents"
  default_prompt: "Use $slackbot-builder to design or harden a Slack bot; pick a maturity level (L0–L6) and follow its reference file."
policy:
  allow_implicit_invocation: true
