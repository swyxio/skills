interface:
  display_name: "Slackbot Builder"
  short_description: "Build and harden production Slack bots and agents"
  default_prompt: "Use $slackbot-builder to route this Slack bot task to the smallest relevant lifecycle or capability guidance."
policy:
  allow_implicit_invocation: true
